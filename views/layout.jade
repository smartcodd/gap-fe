include mixins
doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        meta(http-equiv="X-UA-Compatible", content="ie=edge")
        title Gestón de Agua Potable
        script(src="//ajax.googleapis.com/ajax/libs/jquery/1.12.2/jquery.min.js")
        link(href="//maxcdn.bootstrapcdn.com/bootswatch/3.3.0/flatly/bootstrap.min.css", rel="stylesheet")
        script( src='//maxcdn.bootstrapcdn.com/bootstrap/3.3.1/js/bootstrap.min.js' )
        link(rel="stylesheet", href="/docs/css/app.css")
        script(src="/socket.io/socket.io.js")
        script(src="/docs/js/handlebars.js")
    body(style="")
        +nav("Project name", "expand")
            +nav_item( "/", "active" ) Home
            +nav_item( "/login" ) Iniciar Sessión
            +nav_item( "/logout" ) Salir
            +nav_item_dropdown( "#" )( label="Opciones")
                +nav_item( "/signup" ) Registrarse
                +nav_divider
                +nav_item( "/app/imagenes/new" ) Subir Imagen
                +nav_item( "/app" )  Inicio
                +nav_item( "/app/imagenes" )  Mis Imagenes 
            
        div(class=["container-fluid"] style="padding-top:6rem;",id="container_gap")
            .row.flex-xl-nowrap
                +columnaAll(12,2,2,2,2,2,1,"bd-sidebar")
                    .list-group
                        if listUsers
                            for user in listUsers
                                if user.conected=="S"
                                    a(class=["list-group-item","list-group-item-action","list-group-item-success","#{user._id}"] href="#") #{user.username}
                                        span(class=["badge","badge-primary","badge-pill"]) 1
                                else
                                    a(class=["list-group-item","list-group-item-action","#{user._id}"] href="#") #{user.username} 
                                        span(class=["badge","badge-primary","badge-pill"]) 2
                    include chat
                +columnaAll(12,10,10,10,10,4,5,"bd-content")
                    block contenido
        input(type="hidden"  id="hiddenSecret" value="#{ID}")
                
