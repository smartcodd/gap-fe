mixin nav(name, id, style)
    - var style = (typeof style === 'undefined') ? "default" : style
    nav( role="navigation", class=["navbar", "navbar-" + style,"navbar-fixed-top"] )
        .navbar-header
            button.navbar-toggle.collapsed( type="button", data-toggle="collapse", data-target="#" + id, aria-expanded="false", aria-controls="navbar")
                span.sr-only Toggle navigation
                span.icon-bar
                span.icon-bar
                span.icon-bar
            a.navbar-brand(href="#")= name
        .collapse.navbar-collapse( id=id )
            ul.nav.navbar-nav
                if block
                    block
                else
                    p No content provided
mixin navBar(name,navStyle,fixed)
    nav(class=["navbar","navbar-"+navStyle,"navbar-fixed-"+fixed],role="navigation")
        .navbar-header
            a.navbar-brand(href="#")= name
        if block
            block
        else
            p No content provided

mixin navBarNav(position)
    div(calss=["nav","navbar-nav","navbar-"+position])
        if block
            block
        else
            p No content provided

mixin nav_item(href, active)
    li(class=active): a( href=href )
        if block
            block
        else
            p No content provided
mixin nav_item_dropdown(href, active)
    li(class=["dropdown", active])
        a.dropdown-toggle( href=href, data-toggle="dropdown", role="button", aria-expanded="false" )= attributes.label
            span.caret
        ul.dropdown-menu( role="menu" )
            if block
                block
            else
                p No content provided
 
mixin nav_divider
    li.divider
 
mixin nav_header
    li.dropdown-header
        if block
            block
        else
            p No content provided
//- Panel mixin
mixin panel(heading, style)
    - var style = (typeof style === 'undefined') ? "default" : style
    div( class=["panel", "panel-" + style] )
        .panel-heading= heading
        .panel-body
            if block
                block
            else
                p No content provided
//- Button mixin
mixin button(style, href, size)
    - var style = (typeof style === 'undefined') ? "default" : style
    - var href = (typeof href === 'undefined') ? "#" : href
    case size
        when "large"
            - size = "btn-lg"
        when "small"
            - size = "btn-sm"
        when "mini"
            - size = "btn-xs"
    a( class=["btn", "btn-" + style, size], href=href )
        block
mixin columna(md,sm,xs,style)
    - var style = (typeof style === 'undefined') ? "default" : style
    div( class=["col-md-"+md, "col-sm-"+sm , "col-xs-" + xs] style=style)
        block
mixin columnaAll(col,md,xl,sm,xs,pymd,plmd,styleClss)
    - var styleClss = (typeof styleClss === 'undefined') ? "default" : styleClss
    div( class=["col-"+col,"col-md-"+md,"col-xl-"+xl, "col-sm-"+sm , "col-xs-" + xs,"py-md"+pymd,"pl-md"+plmd,styleClss])
        block

//- Button mixin
mixin button(style,size,typeButon,valueText)
    - var style = (typeof style === 'undefined') ? "default" : style
    - var href = (typeof href === 'undefined') ? "#" : href
    case size
        when "large"
            - size = "btn-lg"
        when "small"
            - size = "btn-sm"
        when "mini"
            - size = "btn-xs"
    input( type=typeButon, class=["btn", "btn-" + style, size] , value=valueText)

mixin link(style,href,size) 
    - var style = (typeof style === 'undefined') ? "default" : style
    - var href = (typeof href === 'undefined') ? "#" : href
    case size
        when "large"
            - size = "btn-lg"
        when "small"
            - size = "btn-sm"
        when "mini"
            - size = "btn-xs"
    a(href=href,class=["btn", "btn-" + style, size] , role="button") 
        block

//- Panel mixin
mixin panel(heading, style)
    - var style = (typeof style === 'undefined') ? "default" : style
    div( class=["panel", "panel-" + style] )
        .panel-heading= heading
        .panel-body
            block
//- End navbar mixins